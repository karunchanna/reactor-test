# Reactor API key (server-side only). Starts with rk_. Get one at https://reactor.inc/dashboard
REACTOR_API_KEY=rk_your_key_here

# OpenAI API key, used server-side to generate the combined seed image (gpt-image-1).
OPENAI_API_KEY=sk-your_key_here
